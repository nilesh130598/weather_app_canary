apiVersion: v1
kind: Service
metadata:
  name: frontendv1-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: frontend
    version: v1
 
  ports:
    port: 80
    targetPort: 80
 

