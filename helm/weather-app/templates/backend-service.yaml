apiVersion: v1
kind: Service
metadata: 
  name: backend-service
  namespace: {{ .Values.namespace }}
spec:
  selector: 
    app: backend 

  ports:
  - port: {{ .Values.service.backend }}
    targetPort: {{ .Values.backend.port }}

  type: ClusterIP